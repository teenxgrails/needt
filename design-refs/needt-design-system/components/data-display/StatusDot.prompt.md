One-line: the small colour dot that precedes a board column title; use it wherever a lane or state needs identifying without a coloured label.

```jsx
<StatusDot status="review" />
```

Amber = To Do, blue = In Progress, violet = In Review, green = Completed. These four values are identical in light and dark themes — they are the only colours in the system that do not flip.
