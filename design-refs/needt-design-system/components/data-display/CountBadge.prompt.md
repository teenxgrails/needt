One-line: the muted count chip that follows a board column heading; never colour it — counts are information, not status.

```jsx
<CountBadge>3</CountBadge>
```

6px radius, `--surface-2` fill, 11px `--text-muted`, no border. Also used for the breadcrumb's terminal segment (with `--text-2` text).
