One-line: the header view switcher — pill-less, underline-less tabs where selection is a filled `--surface-2` chip at 8px radius.

```jsx
<Tabs tabs={["Board", "List", "Timeline", "Due Tasks"]} value={tab} onChange={setTab} />
```

4px gap between tabs, 13px text, inactive `--text-muted`. Labels are Title Case.
