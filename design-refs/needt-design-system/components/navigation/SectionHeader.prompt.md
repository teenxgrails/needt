One-line: the uppercase group label that splits the sidebar nav into sections, with a disclosure chevron and hover-level +/⋯ actions.

```jsx
<SectionHeader label="File" expanded onToggle={toggle} />
```

11px medium, uppercase, `--tracking-wide`, `--text-muted`. Sections are separated by 20px of vertical space, not rules.
