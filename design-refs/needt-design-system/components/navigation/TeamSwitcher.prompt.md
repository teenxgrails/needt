One-line: the sidebar's top workspace switcher — 34px avatar, an 11px "Team" eyebrow over a 13px semibold name, and a chevron-up-down affordance.

```jsx
<TeamSwitcher team="David Visuals" initials="DV" />
```

16px radius, hairline border, transparent fill that becomes `--surface-hover` on hover. It lives in a 12px-padded block above the search field.
