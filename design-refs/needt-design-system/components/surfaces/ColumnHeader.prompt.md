One-line: the board column heading — dot, 13px semibold title, count chip, and right-aligned borderless +/⋯ buttons.

```jsx
<ColumnHeader title="In Review" status="review" count={3} />
```

Columns are 288px wide with a 12px gap between the header and the first card, and 20px between columns.
